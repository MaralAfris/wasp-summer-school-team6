mesh = dict(
        min_dist = 4,
        #padding_radius = 0.175 # same as turtlebot radius
        padding_radius = 0.2
)
plan = dict(
        turtle_speed = 2,
        drone_speed = 3,
        drone_carry_speed = 0.3
)
